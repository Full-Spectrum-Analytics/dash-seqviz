name: Publish to PyPI & npm

on:
  release:
    types: [published]
  workflow_call:

permissions:
  contents: read
  id-token: write  # required for PyPI trusted publishing

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: npm

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install npm dependencies
        run: npm ci

      - name: Build JS bundle
        run: npm run build:js

      - name: Install Python build tools
        run: pip install --upgrade pip build

      - name: Install dash for component generation
        run: pip install -r requirements.txt

      - name: Generate Dash component
        run: npm run build:backends

      - name: Validate package
        run: npm run validate-init

      - name: Build Python sdist and wheel
        run: python -m build

      - name: Upload Python artifacts
        uses: actions/upload-artifact@v5
        with:
          name: python-dist
          path: dist/

      - name: Upload npm artifacts
        uses: actions/upload-artifact@v5
        with:
          name: npm-package
          path: "*.tgz"

  publish-pypi:
    needs: build
    runs-on: ubuntu-latest
    environment: release
    permissions:
      id-token: write  # trusted publishing (no token needed)
    steps:
      - name: Download Python artifacts
        uses: actions/download-artifact@v5
        with:
          name: python-dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        # Uses trusted publishing - configure at pypi.org/manage/project/dash-seqviz/settings/publishing/

  publish-npm:
    needs: build
    runs-on: ubuntu-latest
    environment: release
    steps:
      - uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Build JS bundle
        run: npm run build:js

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
