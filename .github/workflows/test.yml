name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
        node-version: ["18", "20"]
    steps:
      - uses: actions/checkout@v5

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Chrome (for selenium integration tests)
        uses: browser-actions/setup-chrome@v1
        with:
          install-chromedriver: true

      - name: Install npm dependencies
        run: npm ci

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r tests/requirements.txt
          # dash[testing] caps selenium<=4.2.0; upgrade separately for
          # Selenium Manager (auto-provisions a matching chromedriver).
          pip install -U "selenium>=4.25"

      - name: Build component (JS + Python)
        # build:backends runs dash-generate-components, which comes from the
        # dash package installed in the previous step, so deps must be first.
        run: npm run build

      - name: Install the built package
        run: pip install .

      - name: Run Python tests
        run: |
          python test_basic.py
          pytest --headless
