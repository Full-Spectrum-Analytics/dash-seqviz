# Packages needed to run the tests.
# Switch into a virtual environment
# pip install -r requirements.txt

dash[dev,testing]>=3.0.0
biopython>=1.83
# NOTE: dash[testing] pins selenium<=4.2.0, which lacks Selenium Manager.
# Upgrade selenium separately after installing this file (see CI / README):
#     pip install -U "selenium>=4.25"
